query ScheduleList($date: Timestamp!, $id: ID) {
      schedules(date: $date, userId: $id) {
        schedules {
          id
        name
        status
        category {
          color
        }
        dateTimeStart
        dateTimeEnd
        stickerCount
        stickerNames
        }
  }
}
