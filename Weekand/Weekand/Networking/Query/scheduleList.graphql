query ScheduleList($date: Timestamp!) {
    schedules(date: $date) {
        schedules {
          id
        name
        status
        category {
          color
        }
        dateTimeStart
        dateTimeEnd
        stickerCount
        stickerNames
        }
  }
}
